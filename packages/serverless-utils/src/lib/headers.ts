export const HEADERS = Object.freeze({
  RESPONSE: {
    CONTENT_TYPE_APP_JSON: Object.freeze({ 'Content-Type': 'application/json' }),
    CONTENT_TYPE_APP_TEXT_PLAIN: Object.freeze({ 'Content-Type': 'text/plain' }),
    CONTENT_TYPE_APP_JSON_WITH_PROBLEM: Object.freeze({ 'Content-Type': 'application/problem+json' }),
  },
});
